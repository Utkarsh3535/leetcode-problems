#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {


    int N, i, val, k = 0;

  
    scanf("%d", &N);

   
    int arr[N];

   
    for(i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

  
    scanf("%d", &val);

   
    for(i = 0; i < N; i++) {
        if(arr[i] != val) {
            arr[k] = arr[i];
            k++;
        }
    }

   
    for(i = 0; i < k; i++) {
        printf("%d ", arr[i]);
    }

   
    return 0;
}
